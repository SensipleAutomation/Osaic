wait(LogPage.getNewLeadToast());
		WebElement toastMessageElement = getNewLeadToast();
		String actualMessage = toastMessageElement.getText();
		String expectedMessage = "Lead 'AutoJohn Doetest' was created";
		assertEquals("Success message does not match", expectedMessage, actualMessage);
