if (elementText.equals(expectedText)) {

			System.out.println("Element text is as expected: " + elementText);
		} else {
			Thread.sleep(1000);
			WebElement drop = wait.until(ExpectedConditions.visibilityOfElementLocated(Hopage.getBusinessChoose()));
			drop.click();
			WebElement search = wait.until(ExpectedConditions.visibilityOfElementLocated(Hopage.getSerachbar()));
			search.sendKeys("Business");
The method visibilityOfElementLocated(By) in the type ExpectedConditions is not applicable for the arguments (WebElement)
