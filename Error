WebElement textBox;

    if (i == 9) { // Check if it's the 10th question
        textBox = TestUtils.explicit_locate(quesTenText).get(0);
    } else {
        textBox = TestUtils.explicit_locate(backConsentTextbox).get(i - 5);
    }
