Row row = sheet.getRow(startingRow);
        if (row == null) {
            row = sheet.createRow(startingRow);
        }

        int cellIndex = startingCell;
        for (WebElement element : elements) {
            Cell cell = row.createCell(cellIndex++);
            cell.setCellValue(element.getText());
        }
